select-default(){
	border: 1px solid $palette-alpha-border; 
	padding $base-padding ($base-padding/2)
	border-radius($radius-small)
	width: auto
	font-size: 14px;
  	background-color $light
  	background none;
}
.select-default{
	select-default(); 
}